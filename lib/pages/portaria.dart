import 'package:flutter/material.dart';

class PortariaPage extends StatelessWidget {
  const PortariaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Portaria"),
    );
  }
}